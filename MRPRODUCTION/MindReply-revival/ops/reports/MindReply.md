# Report: MindReply

Repository: Mind-Reply/MindReply
Branch: automation/scan-fix--MindReply

Summary of findings (initial scan):
- Plaintext secrets and deploy scripts referencing tokens found in: DEPLOY_NOW.txt, Deploy-AdminDashboard.ps1, ACTIVATE_LIVE.sh, FILES_CREATED.txt. Examples: ADMIN_PASSWORD, ADMIN_SECRET, VERCEL_TOKEN, STRIPE keys, ANTHROPIC_API_KEY, GMAIL tokens.
- Several scripts and deployment helpers assume direct token placement. Some docs include explicit test Stripe keys (STRIPE_CONFIG.md) which must be removed/rotated.
- No vercel.json or unified GitHub Actions workflow present for robust CI/CD in this repo.

Fixes added on branch (automation/scan-fix--MindReply):
1. Removed or replaced plaintext secret examples in tracked files with process.env placeholders and added guidance comments. (Changes live in branch only.)
2. Added `.gitignore` normalization to prevent committing .env, .mcp.env, and other credential files.
3. Added `vercel.json` (framework: nextjs) and a GitHub Actions workflow `.github/workflows/mr-build-deploy.yml` that builds and attempts deployment to Vercel using the org secret `MR_VERCEL_TOKEN`.
4. Added a Dockerfile fallback (Dockerfile.preview) to enable Docker build + container deploy path.
5. Added `ops/reports/MindReply.md` (this file) and `ops/reports/index.md` will be created across repos to centralize reporting.

Secrets removed (names only):
- ADMIN_PASSWORD
- ADMIN_SECRET
- VERCEL_TOKEN
- STRIPE_SECRET_KEY
- STRIPE_WEBHOOK_SECRET
- ANTHROPIC_API_KEY
- GMAIL_ACCESS_TOKEN

Next actions (recommended):
- Review PR: automation/scan-fix--MindReply (do not merge until secrets are re-provisioned as org secrets).
- Populate org secrets with the following names (recommended): MR_VERCEL_TOKEN, MR_STRIPE_SECRET, MR_ANTHROPIC_KEY, MR_GMAIL_TOKEN.
- After secrets are populated, merge the PR to trigger GitHub Actions which will attempt Vercel deploy automatically.

CI / Deploy plan executed by workflow:
1) Build & test
2) If success → Deploy to Vercel using MR_VERCEL_TOKEN
3) If Vercel deploy fails → (fallbacks to Railway/Render/Docker implemented in later PRs)

Report generated by automation on: 2026-07-01T00:00:00Z

---

Commit message: Add ops report and CI/deploy scaffolding (automation/scan-fix--MindReply)
